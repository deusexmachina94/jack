// Aggregated Drizzle schema for BRIP core.
//
// Each module defines its own tables in `../modules/<name>/schema.ts` and
// re-exports them here so drizzle-kit sees the whole schema in one place.
// Empty until Phase 1a introduces the registry tables (publishers, domains).
//
// e.g. export * from '../modules/registry/schema.js';

export {};
